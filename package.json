{
  "name": "ui-game",
  "devDependencies": {
    "webpack": "^5.86.0",
    "webpack-cli": "^5.1.4",
    "html2canvas": "^1.4.1",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "dev": "npx webpack -w & npx http-server -s -p 3000 -o ./test-arena",
    "build": "rm -rf extension/* && cp config/* extension/ && npx webpack"
  }
}
